const username_db = "root";
const password_db = "";
const host_db = "localhost";
const database_name = "be";
const port_db = 3300;

module.exports = {
  username_db,
  password_db,
  host_db,
  port_db,
  database_name,
};
